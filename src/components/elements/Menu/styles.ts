import styled from 'styled-components';

export const Container = styled.div`
    position: absolute; 
    padding: 10px;
    display: flex;
    align-items: center; 
    right: 1.4rem; 
    height: 3rem;
    border-radius: 30rem;
`;